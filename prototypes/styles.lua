local styles = data.raw["gui-style"].default

styles["trainfactory_group_frame"] = {
    type = "frame_style",
    parent = "inside_shallow_frame_with_padding",
    vertically_stretchable = "on"
}

styles["trainfactory_machine_frame"] = {
    type = "frame_style",
    parent = "inside_shallow_frame_with_padding",
    vertically_stretchable = "on"
}

styles["trainfactory_color_textfield"] = {
    type = "textbox_style",
    width = 36,
}
